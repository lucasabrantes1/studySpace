/*Quando criamos uma estrutura da página html toda essa estrutura de tags é chamado DOM
(Document objetc model) - 

(document) --> Quando criamos criamos uma página HTML no topo colocamos o doctype é nessa parte que estamos especificamos o que é aquele documento.

    (Root element: <html> ) --> Elemento raiz do html.

            (Element: <head>) --> Dentro da tag html tem outros elementos também. e o head é um deles, ou seja o head é filho do html, o html é pai do head 
                            filho = elemento dentro de outro elemento  || pai = elemento que tem outros elementos dentro dele.

            (Element: <body>) --> Na mesma estrutura hierarquica existe o body, onde ambos são filhos do elemento html onde o head e body são irmãos porque ambos estão sendo filhos do html 
            e no mesmo nível.



*/